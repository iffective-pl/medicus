services:
  sql:
    image: postgres
    restart: always
    hostname: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: "postgrespw"
  app:
    build:
      context: .
    restart: always
    hostname: medicus
networks:
  default:
    name: application